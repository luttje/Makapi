<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ApiMonkey.Pages.CollectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ApiMonkey.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid Margin="36,24,36,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Saved Collection Info -->
        <Grid Grid.ColumnSpan="2"
              Padding="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Text="{x:Bind CurrentCollection.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="24"
                     FontWeight="Black"
                     Background="Transparent"
                     BorderThickness="0"
                     Margin="-10,0,0,0"
                     MinWidth="400"/>

            <Button Grid.Column="1"
                    Margin="10,0,0,0"
                    Content="Open Collection Folder"
                    Click="OpenCollectionFolderButton_Click"
                    VerticalAlignment="Bottom" />
        </Grid>
    </Grid>
</Page>
